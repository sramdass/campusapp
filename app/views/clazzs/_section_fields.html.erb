<tr class = "tfields">	
	<td><%= f.text_field :name, :size => 15 %></td>	
	<td><%= f.select :faculty_id, options_for_select(@clazz.branch.faculties.collect {|t| [ t.name, t.id ] } , f.object.faculty_id) , {:prompt => "Class teacher"}  %></td>
 	<td class='remove-button'><%= link_to_remove_fields "Delete", f %></td>
</tr>
 
 
	
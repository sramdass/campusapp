<%= form_for([@branch], { :url => { :action => "examcreate" } }) do |f| %>	
    <%= render 'shared/error_messages', :target => @branch %>
	<table class="input_form_table">
		<thead>
	    	<tr>
	    		<!--All the required fields for the Exam model should come here-->
	        	<th scope="col">Name</th>
	        </tr>
	    </thead>
	    <tbody>		
		<%= f.fields_for :exams do |builder| %>	
	 		<%= render "exam_fields", :f => builder %>	 	  
		<% end %>
	    </tbody>		
	</table>			
 	<p class='add-button'> <%= link_to_add_fields "Add Exam", f, :exams %> </p>
 	<p class='button'> <%= f.submit  "Update Exams" %>  </p>
<% end %>
